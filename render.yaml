steps:
  - type: shell
    command: npm install

services:
  - type: web
    name: legal-app-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
